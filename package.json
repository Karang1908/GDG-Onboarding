{
  "name": "council-two-truths",
  "version": "1.0.0",
  "private": true,
  "description": "Two Truths and a Lie - live council onboarding game",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node --check server.js && node build.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "ISC",
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
